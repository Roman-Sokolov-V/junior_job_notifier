# Copy to .env.docker and adjust. Used by docker compose (see docker-compose.yml).

# In Compose, DATABASE_URL is usually overridden by compose — only set if you run the image alone.
# DATABASE_URL=postgresql+psycopg2://scraper:scraper@db:5432/vacancies

TELEGRAM_BOT_TOKEN= # the same as for junior_job_notifier_bot

